{
  "run": [
    {
      "method": "notify",
      "params": {
        "html": "Installing MyCanvas dependencies (npm workspaces). This may take a few minutes."
      }
    },
    {
      "method": "shell.run",
      "params": {
        "message": "npm install"
      }
    },
    {
      "method": "notify",
      "params": {
        "html": "MyCanvas is installed. Click Start to launch the app."
      }
    }
  ]
}
